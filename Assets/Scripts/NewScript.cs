using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("NewScript");
    }

    // Update is called once per frame
    void Update()
    {
        // Debug.Log("NewScript Update");
    }
}
